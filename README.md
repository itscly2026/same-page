# Same Page

> 当前状态：产品与领域设计已经确认，工程实现和技术原型正在启动。本文描述 V1 目标，不代表功能已经上线。

Same Page 是一款面向合唱排练的乐谱 PWA。它让成员围绕同一份 PDF 乐谱工作，通过共享批注层传达排练要求，并通过个人层保留自己的记号。

产品使用同一套响应式 Web/PWA 界面服务平板、桌面和手机，主要使用环境在中国大陆。Cloudflare 国际版的偶发网络不稳定是首版接受的限制，离线能力不得因此被夸大成永久存储保证。

## V1 验收故事

V1 必须完整跑通以下路径：

1. 管理员上传并发布一份 PDF。
2. 获授权编辑者在共享层添加批注。
3. 成员下载离线副本，查看共享批注并在个人层编辑。
4. 断网、刷新或关闭应用不会丢失本地草稿。
5. 恢复网络后，本机待上传操作可靠同步。
6. 不同批注对象的并发修改自动合并；同一对象的后到修改成为提交设备上的本地冲突。

V1 不包含实时协作、WebSocket、自动批注迁移、导出、打印、演出单、文件夹、标签、公开建团、计费或端到端加密。

## 合唱团与加入方式

数据模型从第一天支持多个互相隔离的合唱团，一个用户可以加入多个团；产品入口只按合唱团划分，不提供跨团乐谱、收藏或动态聚合。

首版不开放创建或关闭合唱团的产品入口。部署时预置 **小红花合唱团** 及其首位管理员，未来新团由平台运维命令创建。

每个团有一个八位团邀请码：

- 使用不易混淆的大写字母和数字，由服务端随机生成。
- 同一时间只有一个有效邀请码，管理员可以轮换。
- 未注册访客凭邀请码读取已发布乐谱和全部共享层，不形成成员关系，也不出现在成员列表中。
- 访客可以下载离线副本，但已经下载的内容无法远程追回。
- 访客完成邮箱 OTP 注册并填写团内显示名后，建立普通成员关系。
- 已注册用户可以使用其他团的邀请码加入多个团。

## 身份、成员与权限

Same Page 使用邮箱 OTP 登录，不提供密码登录。认证由 Same Page 自己的 Better Auth/D1 边界负责；登录邮件通过独立的 Resend Sending-only key 和发送域发出，不调用或共享现有 Webmail 的 API、Secret 或 Sent 数据。

权限只在所属合唱团内生效：

- **团管理员**：管理成员、乐谱、共享层、邀请码、配额和共享层编辑权。
- **共享层编辑权**：允许某个成员关系编辑指定共享层，同一层可授权给多人。
- **普通成员**：读取全部共享层，编辑自己的个人层。
- **访客**：读取已发布乐谱与共享层，不能修改云端数据。

“指挥”“声部长”和声部名称不是写死的系统角色。管理员可以自定义共享层，SATB 只是一套初始模板。

同一个用户在不同团可以使用不同的团内显示名。只有管理员可以查看成员名单和登录邮箱；普通成员与访客没有成员目录。共享批注保留创建者和最后修改者的团内显示名，只有管理员和共享层编辑者在对象详情或冲突场景中看到署名。

删除账号会立即禁用登录，并为身份数据和个人层启动三十天恢复期；之后永久删除。共享批注作为团内内容继续保留，并保留作者最后使用的团内显示名。成员被移除或主动退出时立即失去云端访问，服务端个人层同样保留三十天。

## 乐谱与文件

一份乐谱属于且只属于一个合唱团。元数据包括：

- 标题：必填。
- 作曲者、编曲者：选填。
- 管理员排序与归档状态。

成员可以按标题、作曲者和编曲者搜索；V1 不做文件夹、标签、收藏或跨团搜索。

文件规则：

- 只接受 PDF，单份最大 **20 MB**。
- 每个团的全部文件对象共享 **1 GB** 配额。
- 当前 PDF、归档乐谱和保留期内的旧 PDF 版本都计入配额。
- 加密、损坏或 PDF.js 无法解析的文件不能发布。
- 草稿可替换或删除；发布后允许替换 PDF，也允许归档乐谱。

PDF 替换不会尝试理解内容或迁移批注：

- 每次替换生成新的不可变 PDF 版本，并更新乐谱的当前版本指针。
- 上传者预览后确认现有批注仍按原页码和归一化坐标显示。
- 页数减少时，超出范围的批注保留但不可见。
- 离线用户可以继续使用旧版本；进入乐谱或手动同步时提示新版本。
- 新版本完整下载并校验成功后才切换本地副本。
- 旧 PDF 保留三十天用于回滚，之后删除。

## 阅读器

V1 同时支持：

- 单页翻页模式。
- 连续滚动模式，按视口虚拟化 PDF 页面。

两种模式共享同一页坐标和批注数据，用户可以随时切换，并按用户、按乐谱记住选择。阅读器支持缩放、拖动、触摸/鼠标/键盘翻页、页码输入和缩略图导航。

在线阅读使用 HTTP Range。完整离线副本只有在 PDF、批注和必要资源全部写入本机并校验成功后才能标记为“可离线使用”。PWA 安装是强提示建议，不是使用门槛。

## 批注层与工具

每份乐谱可以有多个共享层，以及每名注册用户唯一的个人层。

- 共享层由管理员命名、排序并设置默认颜色。
- 所有成员和访客都可读取全部共享层，并独立控制显示与隐藏。
- 用户可以覆盖共享层在自己界面中的颜色；注册用户偏好跨设备同步，访客偏好只保存在当前设备。
- 任何拥有该层编辑权的成员都可以修改或删除层内任意对象。
- 个人层只允许本人通过产品 API 读取和编辑；V1 采用应用级权限隔离，不做端到端加密。

V1 工具：

- 画笔：支持鼠标、触摸和手写笔，固定线宽为基线，可在设备支持时记录压感。
- 橡皮：整条删除笔迹。
- 文本：新增、编辑、移动和删除。
- 撤销/重做：只作用于当前层和当前编辑会话。

V1 不做形状、荧光笔、套索、多选、图片、激光笔或单对象自定义颜色。

## 本地保存与同步

批注对象是一条具有稳定 ID 的笔迹或文本。绘制期间的修改持续写入 IndexedDB，用户点击“完成”时批量提交：

- 在线无冲突：显示已同步。
- 离线：显示已保存到本机和待同步数量。
- 网络恢复或应用恢复前台时：只重试本机 outbox。
- 进入乐谱或点击“立即同步”时：执行 push 和 pull。
- 不后台轮询其他成员的新批注，不使用实时连接。

每个同步操作包含稳定 `opId`、`annotationId`、`baseVersion`、操作类型和 payload。D1 使用乐观并发控制：

- 不同对象的操作独立接受并自动合并。
- 同一对象版本已变化时，服务端保留已接受版本并拒绝后到操作。
- 被拒绝的版本只留在提交设备，成为本地冲突，不上传云端，也不向其他编辑者展示。
- 提交者可以放弃本地版本、基于当前版本重新应用，或将两份内容保留为两个对象。
- 同一批提交里的非冲突操作照常成功。
- `opId` 唯一约束保证丢失响应后的重试不会产生重复对象。

退出登录或清理离线数据前，未同步操作和本地冲突必须被明确处理或确认丢弃。浏览器数据被清理或设备损坏导致的本地冲突丢失，是 V1 明确接受的限制。

会话自然过期不阻止最后登录用户继续访问本机离线数据，但恢复同步必须重新认证。主动退出登录会清除本机私有数据和权限信息；存在未同步内容时必须先警告。

## 推荐技术边界

生产候选栈：

```text
React + TypeScript + Vite
├── PDF.js：PDF Range、解析、页面渲染和虚拟化
├── SVG overlay + DOM：V1 批注显示与编辑
├── React Aria Components：跨输入方式与无障碍组件
├── Dexie：IndexedDB、本地草稿、outbox、冲突与偏好
├── vite-plugin-pwa：manifest、应用壳和更新流程
└── Zod：客户端、API 和 operation 边界校验
                │
                │ 同源 HTTPS
                ▼
Cloudflare Worker + Hono
├── Better Auth：邮箱 OTP 与会话
├── D1：团、成员、权限、乐谱元数据、批注对象和同步记录
├── R2：不可变 PDF 版本对象
└── Resend：独立 Same Page 登录邮件边界
```

PDF 批注的领域数据不得使用绘图库序列化格式。SVG 必须通过真实高密度笔迹原型；只有它在目标设备上无法达标时才采用 `react-konva`。Workbox Background Sync 只能作为增强能力，不能替代业务 outbox。

## 原型与发布门槛

实现前优先验证：

1. PDF.js + R2 鉴权 Range、连续滚动虚拟化、DPR 与内存回落。
2. SVG 与 `react-konva` 在高密度笔迹、缩放、整条删除和多层显示下的实机差异。
3. D1 对不同对象合并、同对象冲突、重复 `opId` 和游标推进的原子性。
4. 断网、强杀、重启、多标签页、会话过期和恢复同步。
5. 20 MB PDF 的完整下载、校验、缓存替换和存储回收。
6. iPad Safari、Android 平板和桌面浏览器上的真实手写与手势。
7. QQ、163、Gmail 和 Outlook 的 OTP 投递及延迟。

自动化使用 Vitest、React Testing Library、Cloudflare Vitest 插件和 Playwright。Playwright WebKit 不能替代真实 Safari；PWA、Service Worker、存储驱逐和手写笔必须保留人工实机验收。

## 领域与架构记录

- [领域词汇](./CONTEXT.md)
- [架构决策](./docs/adr/)
- [Issue 规范](./docs/agents/issue-tracker.md)

## 相关技术

- [Cloudflare Workers Static Assets](https://developers.cloudflare.com/workers/static-assets/)
- [Cloudflare D1](https://developers.cloudflare.com/d1/)
- [Cloudflare R2](https://developers.cloudflare.com/r2/)
- [PDF.js](https://mozilla.github.io/pdf.js/)
- [Dexie](https://dexie.org/)
- [Progressive Web Apps](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/)
