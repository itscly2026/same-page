# #226 云盘导航与设置 UI 验收

本改动在 #225（PR #228，`9a93607`）的读取资源、阅读偏好持久化和修改/刷新结果区分机制上完成 UI 集成。外层个人菜单继续管理账户、帮助、关于和退出登录；云盘内入口只处理云盘显示名、阅读偏好、本机存储和成员身份退出。

## 行为覆盖

- 云盘抽屉保留返回云盘列表及管理入口；Esc、浏览器返回、可见关闭按钮与焦点恢复均沿用导航机制。
- 显示名独立读取并保留已知值，身份恢复完成只更新操作权限，不重建并关闭用户已打开的个人菜单。
- 本机存储显示总大小、乐谱数量、逐文件校验状态与清理操作。完整校验复用 `useOfflineScore`，存在 Blob 不代表可离线；批量清理部分失败时只重试失败乐谱，保留云端内容及本机草稿、待同步操作、冲突。
- 成员列表移除重复角色，保留“我”和已移除状态；拥有者信息在详情内，具体操作权限和授权范围仍可查。
- 成员与共享层视图使用同一 React Aria 分段选择，选中、禁用、焦点和处理中语义分别表达，支持方向键。
- 个人层日常行包含显示勾选和“公开”switch，说明云盘成员可见或仅自己可见。标题菜单承担单层快捷管理、多层明确目标的管理和恢复；删除前保留未同步内容保护。
- 公开等修改须服务端确认。修改成功但刷新失败时只重试刷新；即使删除后目标行消失，仍保留其名称、反馈与重试入口。
- 手机首页主入口独占首行；示例、安装入口在次行，任一或同时隐藏均自然排布。

## 截图复现

使用仓库现有受控 API/PDF fixtures，原始截图仅写入被忽略的 `artifacts/verification/issue-226/`，不包含生产账号数据。

```sh
# 基线 worktree 固定在 ed42f40259353cebb90e5d9dcc89578fb79137f0，且已安装依赖。
ISSUE_226_BASELINE=/path/to/baseline node scripts/capture-issue-226.mjs before
node scripts/capture-issue-226.mjs after
```

打开 `artifacts/verification/issue-226/index.html` 对比首页、抽屉、个人菜单、成员、共享层、个人层以及本机存储空/有文件状态；覆盖 320、360、390、430、1440px，自动记录横向溢出检查。

## 自动化验证

- 使用与 CI 一致的 Node 24：lint、typecheck、19 项 CI scope 检查、79 项 Node 测试、502 项客户端测试及 109 项 Worker 测试通过。
- 全量视觉/交互回归 72 项：71 项直接通过；新合入的慢网用例迁移显示名菜单定位后单独通过，原慢网、跨标签页与输入保护断言保留。
- 数据库迁移回归、生产构建及 precache 校验通过。
- 初次 smoke 中 16 项通过；本条仅保留 PDF 与存储等流程的证据，不将后续补跑视为一次完整绿灯。
- 初次本机运行遇到旧导航定位及测试进程清理 `kill EPERM`；更新入口并单独复跑，未扩大超时或放宽产品断言。

## 证据边界

以上为本地自动化与桌面 Chromium/WebKit 中的受控检查。未进行本轮生产发布，也未进行真实 iPhone/iPad、真实 PWA 安装或排练验收；窄屏浏览器模拟不替代实机结论。
